function area(length, width){
    return length * width;
}

var area1 = area(10,15);
console.log(area1);

arrowArea = (length, width) => { return length * width; }

var area2 = arrowArea(30, 20);
console.log(area2);